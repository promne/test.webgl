title: Random webgl snippets

github:
  source:
    branch: master

collections:
  projects:
        
defaults:
  - scope:
      path: ""
    values:
      layout: default
  - scope:
      path: "projects"
    values:
      html: true